let express,readdirSync,cors,mongoose;_4a0‍.w("express",[["default",["express"],function(v){express=v}]]);_4a0‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_4a0‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_4a0‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require('morgan');
require("dotenv").config();

const app = express();

// DB connection
mongoose
    .connect(process.env.DATABASE_LOCAL)
    .then(() => console.log("DB Connection Successful..."))
    .catch((err) => _4a0‍.g.console.log("DB Connection Error: ", err));

// Middlewares
app.use(cors());
app.use(morgan('dev'));
app.use(express.json());

// route middleware
readdirSync('./routes').map((r) =>
    app.use('/api', require(`./routes/${r}`))
);

const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}...`));